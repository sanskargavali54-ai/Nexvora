import React from "react";
import { cn } from "../../utils/cn";
import { Cpu, Boxes, Workflow, Sparkles } from "lucide-react";

const ICONS = {
  cpu: Cpu,
  boxes: Boxes,
  workflow: Workflow,
  sparkles: Sparkles,
};

const Badge = ({ children }) => (
  <span className="inline-flex items-center px-2 py-0.5 rounded-full text-[11px] bg-white/10 border border-white/10 text-white/80">
    {children}
  </span>
);

const ServiceCard = ({ title, description, status, icon = "cpu", className }) => {
  const Icon = ICONS[icon] || Cpu;
  return (
    <div
      className={cn(
        "glass gradient-border p-5 h-full flex flex-col gap-3 hover:bg-white/[0.08] transition-colors",
        className
      )}
    >
      <div className="flex items-center gap-3">
        <div className="h-10 w-10 rounded-lg bg-white/5 border border-white/10 flex items-center justify-center">
          <Icon size={18} className="text-white/80" />
        </div>
        <Badge>{status}</Badge>
      </div>
      <h3 className="text-lg font-semibold">{title}</h3>
      <p className="text-white/70 text-sm flex-1">{description}</p>
    </div>
  );
};

export default ServiceCard;
